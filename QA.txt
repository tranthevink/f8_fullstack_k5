1. Trình duyệt cache ảnh từ 1 url là như thế nào, có phải nó tải ảnh đó về local không?

2. Ảnh với mã hóa base64 không được cache bởi trình duyệt là như thế nào? Bản chất uri đã chứa dữ liệu ảnh rồi sao lại phải cache?

3. Chuyện gì xảy ra khi nhúng các thẻ link vào thẻ head
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

4. Khi lồng các phần tử vào nhau, thì kích thước của con phụ thuộc vào cha hay kích thước cha phụ thuộc con


5. <a href=""></a> vs <a href="#"></a>, nên dùng cái nào nếu muốn dùng 1 link rỗng ở đây


6. Làm sao để có front-weight lớn hơn 900